# Fence Frames site — explicitly deferred (v1)

Do **not** build these while completing the marketing shell / W1 geo stubs.

| Item | Why deferred | Wave / note |
|------|--------------|-------------|
| Supabase on `fenceframes` (`supabase.jsw`, save design, quotes) | Runtime not required for soft launch | W8 |
| `/design` filter embed + build catalog data | Needs Supabase builds export | W9 |
| Blueprint UI (`mode=blueprint` shell) | Blocked on Embar blueprint JSON | W4 |
| ARC submit flow + PayHOA API | After blueprint; print via media-engine later | W4b |
| Mega-HOA presets/landings (Ridge, Klahanie, IHCA, …) | D9 exit criteria | W7m |
| Neighborhood URL layer / Sammamish buckets | D2 | v2 |
| Public `/contractors` directory | P2 / handbook | Deferred |
| Heritage JS inside Velo repo | Dual-repo: Public-Website only | Never for v1 |
| Redesign / remove `/configure` embed | Soft-launch live | Locked keep |
| Soft-launch password off / domain cutover | Ops / legal | Owner decision |
| Media-engine runtime coupling | Parallel publisher; JSON/URL only | Contracts only |
| TLB branding on consumer UI | Brand firewall | Never |

When a deferred item becomes active, open a focused chat — do not fold into Home/W1 shell work.
